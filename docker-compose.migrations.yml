version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=DeviceManager_Identity
      - DeviceManager_DB=DeviceManager_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
